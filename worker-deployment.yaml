apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: worker
  name: rabbitmq-worker
  namespace: rabbitmq
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  strategy: {}
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
      - env:
        - name: RABBITMQ_HOST
          value: rabbitmq-master
        - name: PYTHONUNBUFFERED
          value: '1'
        image: rabbitmq-worker:v1
        name: rabbitmq-worker
        resources:
          requests:
            cpu: 10m
          limits:
            cpu: 10m
status: {}
